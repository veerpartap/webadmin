{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User edit</h1>


    {% for flashMessage in app.session.flashbag.get('updated') %}
        <div class="flash-notice">
            {{ flashMessage }}
        </div>
    {% endfor %}

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
